import { PlaceholderPage } from "@/components/shared/placeholder-page";
import { routes } from "@/config/navigation";

export default function KpiPage() {
  return <PlaceholderPage route={routes.performanceKpi} />;
}
